import os, sys
import xmltodict
import pandas
import datetime


class XmlConverterFabric:
    def __init__(self, *args, **kwargs):
        self.xml_dir = "no_file_specified.xml"
        self.excel_dir = "no_file_specified.xlsx"
        self._2d_array = [["parent_node", "child_node", "child_child_node"]]
        self.caps = ["Чтение из файла", "Вывод"]

    def xml_dir(self, xml):
        if os.path.exists()
        self.xml_dir = xml

    def excel_dir(self, excel):
        self.excel_dir = excel

    def fields(self, _2d_array):
        self._2d_array = _2d_array

    def caps(self, caps):
        self.caps = caps

    def convert(self):
        pass



    def run(self, input_dir, output_dir):
        if not self.input_dir:


        import os.path
        (input_dir, os.path.join(output_dir, "Выписки_Здания.xlsx"))
